import 'package:flutter/material.dart';

const String serverUrl = 'http://4.79.101.178:5000';

const double edgeWidth = 3.0;

const double paddingAmount = 8.0;

const double labelFontSize = 20.0;

const double appFormContainerHeight = 50.0;

const double halfPaddingAmount = paddingAmount / 2.0;

const BorderRadius roundedCorners = BorderRadius.all(Radius.circular(10.0));

const EdgeInsets padding = EdgeInsets.all(paddingAmount);

const EdgeInsets rowPadding = EdgeInsets.only(
  right: paddingAmount,
  left: paddingAmount,
  bottom: paddingAmount,
);
